import 'package:flutter/material.dart';

const kPrimaryColor = Color(0xFF00BF6D);
const kSecondaryColor = Color(0xFFFE9901);
const kContentColorLightTheme = Color(0xFF1D1D35);
const kContentColorDarkTheme = Color(0xFFF5FCF9);
const kWarninngColor = Color(0xFF220000);
const kErrorColor = Color(0xFFF03738);

const kDefaultPadding = 20.0;

// === Backgrounds ===
const Color kDarkBackground = Color(0xFF0D1117);
const Color kItemBackground = Color(0x26FFFFFF);
const Color kDotColor = Color(0x335A5C66); // or Color(0xFF5A5C66) for exact RGB
const double kDotSpacing = 20.0; // choose a visually comfortable spacing
const double kDotSize = 1.0;    // small dot for subtle pattern

// === Frosted Glass ===
const double kGlassOpacity = 0.05;
const double kGlassBorderOpacity = 0.2;

// === Buttons ===
const Color kSignInButton = kItemBackground;
const Color kSignUpButton = kItemBackground;
const double kButtonHeight = 50.0;
const double kButtonRadius = 50.0; // fully rounded

// === Social Buttons ===
const Color kSocialTagColor = Colors.blueAccent;
const Color kSocialGoogleColor = Colors.redAccent;
const Color kSocialOtherColor = Colors.greenAccent;

// === Text Styles ===
const TextStyle kAuthTitle = TextStyle(
  color: Colors.white,
  fontSize: 28,
  fontWeight: FontWeight.bold,
);

const TextStyle kAuthLink = TextStyle(
  color: Colors.white,
  decoration: TextDecoration.underline,
);
